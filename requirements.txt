# Python dependencies, pinned to the versions verified locally and used by
# the Streamlit Community Cloud deploy.
#
# IMPORTANT: deploy on Python 3.12. dbt-core 1.9 fails to import on 3.13/3.14
# (mashumaro). Set the Python version to 3.12 in Streamlit Cloud → Advanced
# settings when deploying.
#
# NOTE: dbt-utils / dbt-expectations / dbt-date are dbt packages, NOT pip
# packages. They live in packages.yml and are installed by `dbt deps`
# (run automatically by the app's bootstrap on first deploy).

# dbt + DuckDB adapter
dbt-core==1.9.10
dbt-duckdb==1.9.6

# DuckDB engine (shared by dbt and the Streamlit read-only reader)
duckdb==1.5.2

# Dashboard
streamlit==1.57.0
plotly==6.7.0
pandas==3.0.3
pyarrow==24.0.0
