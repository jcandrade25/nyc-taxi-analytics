# dbt + adapter — pinned to a 1.8.x line that's compatible with the
# dbt_utils / dbt_expectations versions in packages.yml.
dbt-core>=1.8.0,<1.10.0
dbt-duckdb>=1.8.0,<1.10.0

# DuckDB engine. dbt-duckdb pulls a compatible version, but pinning
# explicitly keeps the Streamlit reader on the same build.
duckdb>=1.0.0,<2.0.0

# Streamlit dashboard + Plotly viz (per CLAUDE.md project context).
streamlit>=1.36.0
plotly>=5.22.0
pandas>=2.2.0
pyarrow>=16.0.0
