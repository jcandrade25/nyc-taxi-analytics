# Runtime dependencies for the Streamlit dashboard (what Streamlit Community
# Cloud installs). The deployed app reads the committed parquet mart snapshots
# in app/data/ via DuckDB and does NOT run dbt — so dbt is not needed here.
# For local dbt development (building the warehouse / regenerating the marts),
# install requirements-dev.txt instead.
#
# Pinned to the versions verified locally on Python 3.12.

streamlit==1.57.0
plotly==6.7.0
pandas==3.0.3
duckdb==1.5.2
pyarrow==24.0.0
